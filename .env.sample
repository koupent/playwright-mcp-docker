# =============================================================================
# Playwright MCP Docker Environment Configuration
# =============================================================================

# MCP Server Configuration
# MCP server port configuration
MCP_HOST_PORT=9222

# VNC Authentication
# VNC password (used for both VNC client and noVNC web interface)
VNC_PASSWORD=browser-use

# VNC Server Configuration
# VNC server detailed settings
VNC_DISPLAY=:1
VNC_PORT=5901
VNC_GEOMETRY=1920x1080
VNC_DEPTH=24

# noVNC Configuration
# noVNC proxy settings
NOVNC_PORT=8080
